[package]
name = "dlmod-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde.workspace = true
toml.workspace = true
sha2.workspace = true
zip.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys.workspace = true
